// Package detect turns Razorpay orders, payments, and invoices into riskitem.RiskItem sightings: the three detectors behind the unified revenue-at-risk engine, and the queue collapse that merges the ones that are the same debt.
package detect
