# Phase 0 report

(written at phase end)

Cover: what the exit criteria in `PLAN.md` actually did, the green `go test
./...` output, which magic cards are still unverified, and what phase 1
inherits.
